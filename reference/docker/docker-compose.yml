version: '3.8'

services:
  api:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - lives-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3001

volumes:
  lives-data:
